import { createMuiTheme } from '@material-ui/core'

export const theme = createMuiTheme({
    palette: {
        primary: {
            main: '#ffab73'
        },
        secondary: {
            main: '#ffaec0'
        },
        containerDark: {
            main: '#ffd384'
        },
        containerLight: {
            main: '#fff9b0'
        }
    }
})