import {makeStyles} from '@material-ui/core'

export default makeStyles(theme => ({
    googleContainer: {
        maxWidth: "500px",
        margin: "100px auto",
        height: "500px",
        textAlign: "center"
    }
}))