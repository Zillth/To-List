import {makeStyles} from '@material-ui/core'

export default makeStyles(theme => ({
    avatar: {
        marginRight: "10px",
    },
    toolbar: {
        position: "relative"
    },
    logout: {
        position: "absolute",
        right: "15px"
    }
}))