import { makeStyles } from '@material-ui/core'

export default makeStyles(theme => ({
    itemContainer: {
        margin: "5px",
        textAlign: "left",
        display: "flex",
        alignItems: "center",
    },
    item: {
        width: "100%"
    }
}))